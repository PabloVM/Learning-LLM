[project]
name = "learning-llms"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "tiktoken>=0.12.0",
    "torch>=2.11.0",
]
